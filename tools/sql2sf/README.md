## Comprehensive Python script to convert SQL text to Snowflake SQL standard.

This is very useful when migrating from another SQL-based DB to snowflake. IE, DDL -> Snowflake DDL.

Usage:
```bash
$ python sql2sf.py [input file] [output file]
```